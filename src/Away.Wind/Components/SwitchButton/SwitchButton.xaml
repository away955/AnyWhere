<UserControl x:Class="Away.Wind.Components.SwitchButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Name="TxtLabel"/>
            <TextBlock Name="TxtCheckLabel" Text="关" Margin="5,0"/>
            <CheckBox Name="CBSwitchBtn"
                      Style="{StaticResource MaterialDesignSwitchAccentToggleButton}"
                      Checked="CBSwitchBtn_Checked"
                      Unchecked="CBSwitchBtn_Unchecked"/>
        </StackPanel>
    </Grid>
</UserControl>
