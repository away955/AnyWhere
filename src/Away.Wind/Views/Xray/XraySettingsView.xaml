<UserControl x:Class="Away.Wind.Views.XraySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Away.Wind.Views" 
             xmlns:viewmodels="clr-namespace:Away.Wind.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:XraySettingsViewModel}"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="TabItem" BasedOn="{StaticResource MaterialDesignTabItem}">
                <Setter Property="Height" Value="40"/>
                <Setter Property="HeaderTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <TextBlock Foreground="#333" Text="{Binding}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0,10">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button Content="保存" Width="60" Command="{Binding SaveXrayConfigCommand}"/>
            <Button Content="启动" Width="60" Margin="10,0,0,0" Command="{Binding RunCommand}" />
        </StackPanel>

        <TabControl TabStripPlacement="Left">
            <TabItem Header="日志">
                <local:XrayLogSettings Margin="20"/>
            </TabItem>
            <TabItem Header="接口">
                <local:XrayApiSettings Margin="20"/>
            </TabItem>
            <TabItem Header="DNS">
                <local:XrayDnsSettings Margin="20"/>
            </TabItem>
            <TabItem Header="路由">
                <local:XrayRouteSettings Margin="20"/>
            </TabItem>
            <TabItem Header="本地策略">
                <local:XrayPolicySettings Margin="20"/>
            </TabItem>
            <TabItem Header="入站配置">
                <local:XrayInboundSettings Margin="20"/>
            </TabItem>
            <TabItem Header="出站配置">
                <local:XrayOutboundSettings Margin="20"/>
            </TabItem>
            <TabItem Header="网络连接">
                <local:XrayTransportSettings Margin="20"/>
            </TabItem>
            <TabItem Header="流量统计">
                <local:XrayStatsSettings Margin="20"/>
            </TabItem>
            <TabItem Header="反向代理">
                <local:XrayReverseSettings Margin="20"/>
            </TabItem>
            <TabItem Header="FakeDNS 配置">
                <local:XrayFakednsSettings Margin="20"/>
            </TabItem>
            <TabItem Header="Metrics 配置">
                <local:XrayMetricsSettings Margin="20"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
