<UserControl x:Class="Away.Wind.Views.Xray.XrayServerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:viewmodels="clr-namespace:Away.Wind.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:XrayServerViewModel}"
             mc:Ignorable="d" >
    <Grid>
        <StackPanel>
            <Button Content="更新" Width="60" Command="{Binding UpdateNodeCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
