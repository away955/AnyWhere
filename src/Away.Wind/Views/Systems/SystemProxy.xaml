<UserControl x:Class="Away.Wind.Views.SystemProxy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Away.Wind.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:SystemProxyViewModel}"           
             mc:Ignorable="d"
             Width="400" Height="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <CheckBox Content="系统代理" IsChecked="{Binding IsEnable}" />
            <TextBox materialDesign:HintAssist.Hint="代理地址" Text="{Binding Server}"/>
            <TextBox materialDesign:HintAssist.Hint="白名单" 
                     AcceptsReturn="True"            
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Text="{Binding WhiteList}"/>
        </StackPanel>
        <Button Grid.Row="1" Content="保存" Width="60" Command="{Binding SaveCommand}" />
    </Grid>

</UserControl>
