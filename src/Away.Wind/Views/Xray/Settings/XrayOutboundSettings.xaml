<UserControl x:Class="Away.Wind.Views.XrayOutboundSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Away.Wind.Views.Xray.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:XrayOutboundSettingsVM}"
             mc:Ignorable="d" >

    <DataGrid Grid.Row="1" ItemsSource="{Binding Outbound}">
        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding tag}" Header="标识" />
            <DataGridTextColumn Binding="{Binding protocol}" Header="协议" />
        </DataGrid.Columns>
    </DataGrid>


</UserControl>
